name: Build and Publish Docker Image

on:
  push:
    branches:
      - main
    pull_request:
      - main
jobs:

  build-and-publish:
    runs-on: ubuntu-latest
    env:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: 17

      - name: Build with Maven
        run: mvn clean install

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/demo-github-actions:${{ github.sha }} .
          docker tag ${{ secrets.DOCKER_USERNAME }}/demo-github-actions:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/demo-github-actions:latest

      - name: Push Docker image
        uses: docker/login-action@v1
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ env.DOCKER_PASSWORD }}
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/demo-github-actions:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/demo-github-actions:latest
